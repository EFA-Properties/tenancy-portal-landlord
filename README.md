# tenancy-portal-landlord

Tenancy Portal - Landlord Dashboard built with React + Vite + TypeScript + Supabase.

## Setup

```bash
npm install
cp .env.example .env.local
# Add your Supabase URL and anon key to .env.local
npm run dev
```
